{
  "name": "gemic-io",
  "version": "1.0.0",
  "private": true,
  "description": "gemic.io landing page",
  "scripts": {
    "start": "serve -s public -l tcp://0.0.0.0:${PORT:-3000}"
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
